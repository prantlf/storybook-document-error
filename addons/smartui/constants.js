export const ADDON_ID = 'smartuiSettings';
export const PANEL_ID = `${ADDON_ID}/panel`;

export const EVENTS = {
  PREPARE: `${ADDON_ID}:prepare`,
  SET_THEME: `${ADDON_ID}:setTheme`,
  UPDATE: `${ADDON_ID}:update`
};
